// $request->isRetryAdmitted();         // Returns whether retry is admitted {boolean}
// $request->getTransactionId();        // Returns transaction id (unique transaction identifier) {string}
// $request->getCustomerMessage();      // Returns customer message {string}


// $request->callPaymentInit($headModel);                           // Initializes transaction                // Returns RatePAY transaction id (unique identifier)
// $request->callPaymentRequest($headModel, $contentModel);         // Requests authorization of order        // Returns RatePAY descriptor
// $request->callPaymentConfirm($headModel);                        // Confirms authorized transaction !OPTIONAL!
// $request->callConfirmationDeliver($headModel, $contentModel);    // Captures order
// $request->callPaymentChange($headModel, $contentModel);          // Changes order details - subtypes: CANCEL, RETURN, CHANGE_ORDER, CREDIT

// $request->callCalculationRequest($headModel);                    // Requests an installment plan           // Returns installment plan
// $request->callConfigurationRequest($headModel);                  // Requests the installment configuration // Returns merchant installment conditions (in case if installment)

// $request->callPaymentQuery($headModel, $contentModel);           // Pre-requests authorization or order    // Allowed payment methods for current order
// $request->callProfileRequest($headModel);                        // Requests the merchant profile          // Returns merchant configuration and installment conditions (in case if installment)